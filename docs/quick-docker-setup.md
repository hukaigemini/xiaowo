# 🐳 快速Docker配置指南

## 🎯 配置目标
在5分钟内配置必需的Docker Secrets，让CI/CD流水线可以构建和推送Docker镜像。

## 📋 需要配置的Secrets

| Secret名称 | 值 | 说明 |
|------------|----|----|
| `DOCKER_REGISTRY` | `docker.io` | Docker Hub地址 |
| `DOCKER_USERNAME` | 您的Docker用户名 | 登录用户名 |
| `DOCKER_PASSWORD` | Docker访问令牌 | **不是密码** |

## 🚀 第一步：获取Docker访问令牌

### 1.1 登录Docker Hub
1. 打开 https://hub.docker.com
2. 使用您的账户登录
3. 如果没有账户，请先注册一个

### 1.2 创建访问令牌
1. 点击右上角头像
2. 选择 "Account Settings"
3. 在左侧菜单中找到 "Security"
4. 点击 "Access Tokens"
5. 点击 "New Access Token" 按钮
6. 令牌描述：`CI/CD for xiaowo project`
7. 权限选择：`Read, Write & Delete`
8. 点击 "Generate"
9. **重要：立即复制生成的令牌**（只显示一次）

## 🔐 第二步：在GitHub中配置Secrets

### 2.1 进入GitHub Secrets设置
1. 在GitHub中打开您的`xiaowo`仓库
2. 点击顶部的 `Settings` 标签
3. 左侧菜单 → `Secrets and variables`
4. 选择 `Actions`

### 2.2 配置第一个Secret
点击 `New repository secret`

**Secret 1:**
- Name: `DOCKER_REGISTRY`
- Secret: `docker.io`
- 点击 `Add secret`

**Secret 2:**
- Name: `DOCKER_USERNAME`
- Secret: 您的Docker用户名（例如：`myusername`）
- 点击 `Add secret`

**Secret 3:**
- Name: `DOCKER_PASSWORD`
- Secret: 刚才复制的Docker访问令牌
- 点击 `Add secret`

## ✅ 第三步：验证配置

配置完成后，Secrets页面应该显示3个已配置的Secrets：

```
DOCKER_REGISTRY    ✅ Recently updated
DOCKER_USERNAME    ✅ Recently updated  
DOCKER_PASSWORD    ✅ Recently updated
```

## 🔍 第四步：运行测试

我们之前创建的工作流可以验证配置是否正确：

1. 进入GitHub Actions页面
2. 找到 `🔧 Secrets配置测试` 工作流
3. 点击 `Run workflow`
4. 等待运行完成（大约2-3分钟）

### 期望的结果
✅ 如果配置正确，您应该看到：
- `✅ DOCKER_REGISTRY: docker.io`
- `✅ DOCKER_USERNAME 已设置`
- `✅ DOCKER_PASSWORD 已设置`
- `✅ Docker登录成功`

❌ 如果有问题，您会看到具体的错误信息

## 🎉 完成！

恭喜！您已经完成了Docker配置。现在您的CI/CD流水线可以：
- 构建Docker镜像
- 登录Docker Hub
- 推送镜像到仓库

## 📞 遇到问题？

常见问题和解决方案：

**问题1：Docker登录失败**
- 检查用户名是否正确
- 确认令牌没有被截断
- 确认令牌权限为"Read, Write & Delete"

**问题2：找不到Access Tokens**
- 确保在Docker Hub的Account Settings中
- 可能需要先验证邮箱

**问题3：令牌显示过期**
- 令牌只显示一次，生成后立即复制
- 如果丢失，需要重新生成

---

**下一步**: Docker配置完成后，我们可以继续配置其他Secrets或运行完整的CI/CD流水线测试！